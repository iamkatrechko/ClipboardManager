<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.iamkatrechko.clipboardmanager">

    <uses-permission android:name="android.permission.GET_CLIPS"/>
    <uses-permission android:name="android.permission.READ_CLIPS"/>
    <uses-permission android:name="android.permission.WRITE_CLIPS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ClipEditActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <service android:name=".services.ClipboardService">
        </service>
        <service android:name=".services.HideNotificationService">
        </service>
        <service android:name=".services.FloatingViewService">
        </service>
        <service android:name=".services.FloatingCancelViewService">
        </service>

        <provider
            android:name=".data.ClipboardContentProvider"
            android:authorities="com.iamkatrechko.clipboardmanager.data"/>

        <activity android:name=".DeveloperActivity">
        </activity>
        <activity
            android:name=".EditCategoriesActivity"
            android:label="@string/title_activity_edit_categories"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".SearchActivity"
            android:label="@string/title_activity_search"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>